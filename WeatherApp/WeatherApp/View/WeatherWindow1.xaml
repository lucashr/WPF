<Window x:Class="WeatherApp.View.WeatherWindow1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherApp.View"
        mc:Ignorable="d"
        Title="WeatherWindow" Height="450" Width="324.8">
    <Grid>
        <StackPanel HorizontalAlignment="Left" Height="312" Margin="19,16,0,0" VerticalAlignment="Top" Width="274">
            <TextBlock Text="Search for a city"/>
            <TextBox/>
            <Button Margin="0,10" Content="Search"/>
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <Grid Grid.Row="1" 
              Background="#4392f1">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
                  HorizontalAlignment="Left" Height="70" Margin="0,350,0,0" VerticalAlignment="Top" Width="318"/>

    </Grid>
</Window>
